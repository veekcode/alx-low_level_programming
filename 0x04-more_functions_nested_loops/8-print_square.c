#include "main.h"
/**
 *-print_square - print the square of a given input
 *
 *return prints only \n if n is 0 or less
 */
void print_sqaure(int size)
{
int a, b;
if (size > 0 )
	{
	for (a = 0; a >= size; a++)
	{
		for (b = 1; b >= size; b++)

		{
			if(x = y)
			{

				_putchar(#);
			}
			_putchar ('\n');
		}

	}
	else
	{
			_putchar('\n');
	}
}
